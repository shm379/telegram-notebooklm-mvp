# Changelog

**Languages / زبان‌ها:** [English](CHANGELOG.md) · [فارسی](CHANGELOG.fa.md) · [العربية](CHANGELOG.ar.md) · [Español](CHANGELOG.es.md) · [简体中文](CHANGELOG.zh.md)

## استخراج DOCX / XLSX للمستندات المُعاد توجيهها (2026-06-17)

استخراج النص من مستندات Office المُعاد توجيهها، يُنفَّذ محلياً ودون مفتاح API.

### السلوك
- إعادة توجيه ملف `.docx` أو `.xlsx` (يُكتشَف عبر الامتداد أو نوع MIME) يستخرج الآن نصه ويخزّنه في صندوق وارد قابل للبحث. وعلى عكس OCR/PDF الذي يتطلب Gemini، فإن هذا المسار **محلي بالكامل** (لا حاجة إلى مفتاح API أو شبكة).

### التصميم
- وحدة `office.py` نقية مع `detect_office_kind` و`extract_docx_text` و`extract_xlsx_text` والموزّع `extract_office_text` — باستخدام المكتبة القياسية فقط (`zipfile` + `xml.etree`). تُجرى مطابقة الوسوم على الاسم المحلي بحيث يُدعَم كلا متغيري مجال الأسماء (الانتقالي/الصارم).
- DOCX: نص الفقرات (بما في ذلك داخل الجداول) عبر دمج المقاطع (runs). XLSX: تحليل sharedStrings + السلاسل المضمّنة (inline strings) + الأرقام، مع فواصل التبويب/السطر الجديد وفصل أوراق العمل.
- يعيد `NotebookBot._media_route` المسار الجديد `"office"`، ويشغّله `_process_forwarded_media` دون الحاجة إلى خدمة أو `enabled`.

### الاختبارات
- `tests/test_office.py`: كشف النوع، استخراج DOCX (دمج المقاطع/الفقرات)، XLSX (مشترك/مضمّن/رقم، أوراق عمل متعددة، sharedStrings مفقود)، ورفض صيغة غير معروفة.
- `tests/test_inbox_media.py`: توجيه مستندات Office والتنسيق الكامل دون أي خدمة.

## 0.3.0 — مجموعة ميزات Notebook (2026-06-17)

ملخص الإصدار: بالإضافة إلى نواة الـ MVP (الاستيعاب، التفريغ النصي، البحث/السؤال، صندوق الوارد للرسائل المُعاد توجيهها، محرّك القواعد، مهام الاستيراد، MCP)، أُضيف ما يلي:

- البنية التحتية: CI (ruff + pytest) عند كل دفع/PR.
- التنظيم: تجميع المواضيع (`/topics` مع تصنيف بنموذج لغوي كبير)، `/timeline`، المجموعات (`/collection`، بالإضافة إلى `/summarize`/`/export --collection`)، إدارة الوسوم (`/tag rename|delete`).
- المحتوى: معالجة الوسائط المُعاد توجيهها (التفريغ + OCR/PDF + استخراج DOCX/XLSX محلياً)، قواعد الذكاء الاصطناعي (`/rule add-ai`) والوسم التلقائي الاختياري (`/airules`)، الإعادة التلقائية للتوجيه إلى قناة أرشيف (`/setarchive`).
- المخرجات/المراجعة: `/digest` و`/export` (Markdown) و`/stats` و`/recent`، ونقاط النهاية على الويب `/api/{stats,recent,timeline}` مع لوحة مكتبة في لوحة التحكم.
- أدوات MCP جديدة: `list_topics` و`timeline` و`archive_stats` و`list_recent`.

دون تبعيات جديدة؛ مجموعة اختبارات كاملة. تفاصيل كل عنصر في المدخلات أدناه.

## الوسم التلقائي الاختياري بالذكاء الاصطناعي على الرسائل المُعاد توجيهها (2026-06-12)

إكمال قواعد الذكاء الاصطناعي: تشغيلها تلقائياً على الرسائل المُعاد توجيهها الجديدة، على أساس اختياري.

### السلوك
- `/airules on|off` (معطّل افتراضياً) يتحكم في ما إذا كانت قواعد الذكاء الاصطناعي تُشغَّل تلقائياً على كل رسالة مُعاد توجيهها جديدة. عند التشغيل، يُجرى استدعاء واحد للنموذج اللغوي لكل عنصر؛ **ولا تُصنَّف عمليات استيراد القنوات المجمّعة تلقائياً أبداً** (تكلفة محكومة).
- يتطلب مفتاح Gemini؛ وإذا لم يكن هناك مفتاح عند التشغيل، يتم إخطارك.

### التصميم
- اكتسب `IngestionPipeline` معاملاً اختيارياً `ai_classifier`؛ ويطبّق `_apply_rules` أيضاً قواعد الذكاء الاصطناعي عند وجوده (مع ابتلاع الأخطاء). يربطه مسار صندوق الوارد للرسائل المُعاد توجيهها فقط (وفقط عندما يكون المستخدم قد اختار التفعيل ولديه مفتاح).
- عمود `ai_autotag` على `bot_users` مع ترحيل متماثل؛ و`Repository.set_ai_autotag` والمساعد `_ai_classifier_for_user`.

### الاختبارات
- `tests/test_ai_autotag.py`: تطبيق قواعد الذكاء الاصطناعي فقط مع مصنّف، ابتلاع أخطاء المصنّف، استمرار الإعداد، البوابة في `_ai_classifier_for_user`، ومعالج `/airules`.

## الاستعلام عن المجموعات (2026-06-12)

إكمال المجموعات: يمكن الآن تلخيص أو تصدير دفتر ملاحظات.

### السلوك
- `/summarize --collection <name>` و`/export --collection <name>` يلخّصان أو يصدّران إلى Markdown جميع العناصر التي تحمل أياً من وسوم المجموعة (من `items_for_tags`). إذا لم تكن المجموعة موجودة، يُعرَض رسالة مناسبة.

### المكوّنات
- المساعد النقي `NotebookBot._extract_collection(args)` (تحليل العلم `--collection <name>`) و`_collection_items` (تحليل المجموعة إلى عناصر + تسمية نطاق).

### الاختبارات
- `tests/test_collections.py`: `_extract_collection`، و`/summarize --collection` (اتحاد الوسوم ومجموعة مفقودة)، و`/export --collection` (محتوى المستند الصحيح).

## المجموعات / دفاتر الملاحظات (2026-06-12)

تجميع عدة وسوم تحت "دفتر ملاحظات" واحد (مجموعة).

### السلوك
- `/collection new <name>` (اسم من كلمة واحدة)، `/collection add <name> <tag>`، `/collection list`، `/collection remove <name>`، و`/collection show <name>`، الذي يعرض العناصر التي تحمل أياً من وسوم المجموعة (متمايزة، الأحدث أولاً). كلها مقيّدة بنطاق المالك.

### المكوّنات
- جدولا `collections` و`collection_tags` (مع فهرس فريد على `(owner_id, name)`).
- طرق Repository: `create_collection`، `add_collection_tag`، `list_collections`، `collection_tags`، `remove_collection`، و`items_for_tags(owner_id, tags, limit)` (اتحاد متمايز).

### الاختبارات
- `tests/test_collections.py`: CRUD وإضافة الوسوم، العزل لكل مستخدم، `items_for_tags` (اتحاد/تمايز/نطاق)، ومسارات المعالج الكاملة (new/add/list/show/remove + الأخطاء).

## لوحة المكتبة في لوحة التحكم (2026-06-12)

- اكتسبت لوحة التحكم على الويب بطاقة "Library" تستدعي، عند النقر على زر، `/api/stats` و`/api/recent` وتعرض ملخص الأرشيف (عدد العناصر/المصادر/الوسوم وأنواع الوسائط) وأحدث العناصر.
- اختبار دخان (smoke test) في `tests/test_web_api.py` يتحقق من وجود اللوحة والإشارات إلى نقاط النهاية في `INDEX_HTML`.

## Web API: stats / recent / timeline (2026-06-12)

تكافؤ لوحة التحكم على الويب مع القدرات الجديدة (طبقة JSON API).

### السلوك
- ثلاث نقاط نهاية للقراءة فقط `GET /api/stats` و`GET /api/recent?limit=N` و`GET /api/timeline?granularity=month|day` تعيد أرشيف لوحة التحكم (المالك الثابت `0`). ومثل بقية الـ API، فهي محمية بـ `WEB_API_TOKEN` (أو الوصول المحلي loopback عند عدم تعيين رمز) وتستخدم طرق Repository نفسها والدوال النقية `recent_rows`/`build_timeline`/`archive_stats`.
- المساعد `_query_int` للقراءة الآمنة والمقيّدة لمعاملات الاستعلام الرقمية.

### خارج النطاق (متابعة لاحقة)
- عرض هذه البيانات في واجهة لوحة التحكم HTML (في الوقت الحالي، JSON API فقط).

### الاختبارات
- `tests/test_web_api.py`: مخرجات `/api/stats` و`/api/recent` (حد limit والترتيب) و`/api/timeline`، ومتطلب التوثيق عند عدم كونه محلياً.

## تصفّح العناصر الأحدث (2026-06-12)

تصفّح سريع لأحدث العناصر — مكمّل لـ `/timeline` و`/digest`.

### السلوك
- `/recent [n]` (10 افتراضياً، 50 كحد أقصى) يعرض أحدث العناصر مع المصدر والتاريخ والمقتطف والرابط. وتعيد أداة MCP `list_recent` القائمة نفسها.

### المكوّنات
- وحدة `recent.py` النقية مع `recent_rows(items, *, limit, snippet_chars)` (تطبيع المسافات وتقصير المقتطف)؛ تُغذّى من `timeline_items` (الأحدث أولاً).

### الاختبارات
- `tests/test_recent.py`: تطبيع/حد `recent_rows`، مصدر غير معروف، المعالج (ترتيب جديد→قديم وأرشيف فارغ)، وأداة MCP.

## إدارة الوسوم (2026-06-12)

إدارة الوسوم اليدوية (إعادة تسمية / دمج / حذف).

### السلوك
- `/tag rename <old> -> <new>` يعيد تسمية وسم؛ وإذا كان `<new>` موجوداً بالفعل، يُدمَج الوسمان (دون خطأ مفتاح مكرر). `/tag delete <tag>` يزيل الوسم من جميع العناصر. كلاهما مقيّد بنطاق المالك.

### المكوّنات
- `Repository.rename_tag` (INSERT OR IGNORE ثم DELETE لدمج آمن) و`Repository.delete_tag`.

### الاختبارات
- `tests/test_tag_management.py`: إعادة التسمية، الدمج في وسم موجود، الحذف، العزل لكل مستخدم، ومسارات المعالج (rename/delete/usage/missing).

## خلاصة الأحدث (2026-06-12)

"أخبرني بما فاتني" — ملخص بالذكاء الاصطناعي للمحتوى الحديث.

### السلوك
- `/digest [days]` (7 افتراضياً، المدى من 1 إلى 90): يلخّص المحتوى المسجّل خلال آخر N يوماً باستخدام محرّك `summarize` نفسه. وبدون مفتاح Gemini، يعود إلى ملخص بسيط (عدد العناصر + المصادر)؛ وعند عدم وجود محتوى حديث، يعرض رسالة مناسبة.

### المكوّنات
- `Repository.recent_items(owner_id, since_date, limit)` — العناصر التي لها `message_date >= since` (الأحدث أولاً).

### الاختبارات
- `tests/test_digest.py`: تصفية التاريخ/المالك في `recent_items`، ومسارات المعالج (لا محتوى، العودة دون مفتاح، واستخدام summarize مع مفتاح).

## إحصائيات الأرشيف (2026-06-12)

نظرة عامة على الأرشيف عبر `/stats` وأداة MCP `archive_stats`.

### السلوك
- `/stats` يعرض عدد العناصر والمصادر والوسوم، والأعداد حسب نوع الوسائط، والمدى الزمني (التاريخ الأول/الأخير). وتعيد أداة MCP `archive_stats` المخرجات نفسها.

### المكوّنات
- `Repository.archive_stats(owner_id)` مع استعلامات تجميعية (مقيّدة بنطاق المالك).
- وحدة `stats.py` النقية مع `format_stats(stats)`.

### الاختبارات
- `tests/test_stats.py`: التنسيق (فارغ/معبّأ)، التجميع والنطاق لكل مستخدم في `archive_stats`، وأداة MCP.

## تصدير Markdown (2026-06-12)

تصدير الأرشيف إلى ملف Markdown قابل للتنزيل.

### السلوك
- `/export [--source <url>] [--tag <tag>]` يحوّل الأرشيف بأكمله، أو مصدراً واحداً، أو وسماً واحداً، إلى مستند Markdown (مع عنوان ومصدر ورابط ونص لكل عنصر) ويرسله إلى المستخدم كملف.

### المكوّنات
- وحدة `export.py` النقية مع `build_markdown_export(scope_label, items)`.
- `TelegramBotApi.send_document` لرفع الملف.
- المعالج `_handle_export`، الذي يكتب المستند إلى ملف مؤقت، ويرسله، وينظّف بعده.

### الاختبارات
- `tests/test_export.py`: بنية Markdown والحقول المفقودة، وتنسيق المعالج (إرسال المستند بالمحتوى الصحيح، ورسالة الأرشيف الفارغ).

## تصنيفات المواضيع بنموذج لغوي كبير (2026-06-12)

تسمية عناقيد `/topics` بنموذج لغوي كبير (إذا توفّر مفتاح Gemini).

### السلوك
- `/topics` وأداة MCP `list_topics` تبنيان الآن تصنيف كل عنقود باستدعاء واحد للنموذج اللغوي (Gemini) استناداً إلى نصوص عيّنة من العنقود؛ وبدون مفتاح، أو عند خطأ/استجابة فارغة، تعودان إلى تصنيف قائم على أكثر المصطلحات الدالّة تكراراً (`top_terms`). ومخرجات البوت مُهرَّبة بصيغة HTML.

### التصميم
- في `clustering.py`: الدالتان النقيتان `build_label_prompt` و`parse_topic_label`، و`label_cluster(texts, *, generate)` مع استدعاء نموذج لغوي محقون؛ واكتسب `build_topics` معاملاً اختيارياً `namer` يبني تصنيفاً لكل عنقود ويعود عند الخطأ/الفراغ.
- في `bot.py` و`mcp_server.py`، لا يُبنى الـ namer إلا عند وجود مفتاح Gemini.

### الاختبارات
- `tests/test_clustering.py`: بناء/تحليل موجّه التصنيف، `label_cluster` مع generate محقون، و`build_topics` مع namer (التصنيف، والعودة عند الخطأ/الفراغ).

## معالجة الوسائط المُعاد توجيهها (2026-06-12)

إكمال صندوق الوارد للرسائل المُعاد توجيهها: تُنزَّل الوسائط المُعاد توجيهها وتُحوَّل إلى نص قابل للبحث.

### السلوك
- تُفرَّغ ملفات audio/video/voice/video_note تلقائياً (نفس `TranscriptionService`)، وتُحوَّل الصور ومستندات PDF/الصور إلى نص عبر OCR (Gemini متعدد الوسائط). يُخزَّن النص المستخرَج في صندوق الوارد، ويُوسَم، ويُضمَّن، ويصبح قابلاً لـ `/search`/`/ask` (ويُعاد توجيهه أيضاً تلقائياً إذا طابق وسم).
- إذا لم يكن هناك مفتاح Gemini، أو كان نوع الوسائط غير مدعوم، يُخطَر المستخدم ويُخزَّن المرجع/التسمية التوضيحية فقط.

### المكوّنات
- `TelegramBotApi.get_file` + `download_file` (و`file_base_url`) لتنزيل الملف من Bot API.
- `provider_http.gemini_extract_document` (استخراج OCR/نص متعدد الوسائط) والخدمة الرفيعة `ExtractionService`، على قدم المساواة مع `TranscriptionService`.
- في `bot.py`: المساعدان النقيان `_forward_file_ref` (اختيار الملف، أكبر حجم صورة) و`_media_route` (مسار transcribe/extract)، ونواة التنسيق `_process_forwarded_media` مع حقن الخدمة والتنزيل لاختبار غير متصل بالكامل.

### خارج النطاق (متابعة لاحقة)
- استخراج DOCX/Excel ومعالجة الوسائط في مسار الاستيراد الكامل للقناة.

### الاختبارات
- `tests/test_inbox_media.py`: اختيار الملف والتوجيه، تنسيق transcribe/extract، الرفض عند غياب خدمة/مسار/تنزيل، ابتلاع أخطاء الخدمة، و`file_base_url`.

## القواعد القائمة على الذكاء الاصطناعي (2026-06-12)

قواعد وسم قائمة على نموذج لغوي كبير، إلى جانب قواعد الكلمات المفتاحية الموجودة.

### السلوك
- `/rule add-ai <criterion> -> <tag>` يعرّف قاعدة بمعيار بلغة طبيعية؛ ويعرض `/rule list` نوع كل قاعدة برمز (📝 كلمة مفتاحية / 🤖 ai).
- تُقيَّم قواعد الذكاء الاصطناعي فقط أثناء `/rule apply` (استدعاء واحد للنموذج اللغوي لكل عنصر، يغطي جميع قواعد الذكاء الاصطناعي). وبدون مفتاح Gemini تُتجاهل، ويُبلَّغ عن ذلك في المخرجات. وتُطبَّق قواعد الكلمات المفتاحية عند كل استيعاب كما في السابق.
- يتخطّى `match_tags` الآن قواعد الذكاء الاصطناعي في المسارات التلقائية.

### التصميم
- وحدة `rules.py` مع الدالتين النقيتين `build_classify_prompt` و`parse_classified_tags`، و`classify_ai_tags(text, ai_rules, *, generate)`، التي تحقن استدعاء النموذج اللغوي ليبقى قابلاً للاختبار دون اتصال بالكامل.
- عمود `kind` على جدول `rules` مع الترحيل المتماثل `_ensure_rule_columns`؛ و`add_rule`/`list_rules` مع دعم `kind`.

### خارج النطاق (متابعة لاحقة)
- تطبيق قواعد الذكاء الاصطناعي تلقائياً عند كل استيعاب (في الوقت الحالي، `/rule apply` فقط).

### الاختبارات
- `tests/test_ai_rules.py`: تخطّي قواعد الذكاء الاصطناعي في `match_tags`، بناء/تحليل الموجّه، `classify_ai_tags` مع generate محقون وإنهاء مبكر، استمرار `kind`، و`/rule apply` مع مزيج كلمة مفتاحية+ذكاء اصطناعي (نموذج لغوي وهمي) وتخطّي الذكاء الاصطناعي دون مفتاح.

## الخط الزمني (2026-06-11)

عرض زمني للأرشيف — المكمّل الزمني لتجميع المواضيع.

### السلوك
- وحدة `timeline.py` جديدة (Python نقية، دون تبعيات): `build_timeline` يجمّع العناصر المؤرَّخة في سلال تقويمية (شهر `YYYY-MM` أو يوم `YYYY-MM-DD`) ويوفّر أعداداً/مصادر/عيّنة لكل فترة؛ ولأن التواريخ بصيغة ISO 8601، فإن السلة هي ببساطة بادئة تاريخ. تُهمَل التواريخ غير الصالحة.
- `Repository.timeline_items` يعيد العناصر التي لها `message_date` (مقيّدة بنطاق المالك + المصدر/الوسم، الأحدث أولاً).
- أمر البوت `/timeline [--source <url>] [--tag <tag>] [--day]` (الشهر افتراضياً) وأداة MCP `timeline`. وتُهرَّب حقول المستخدم في المخرجات بـ `html.escape`.
- جرى تحديث `/help` وREADME وCHANGELOG.

### الاختبارات
- `tests/test_timeline.py`: `period_key` (السلة ورفض تاريخ سيئ)، التجميع شهر/يوم والترتيب التنازلي، النطاق وترتيب `timeline_items`، وأداة MCP `timeline`.

## إصلاح: تهريب HTML لإعادات التوجيه إلى الأرشيف (2026-06-11)

- لأن `send_message` يرسل بـ `parse_mode: HTML`، فإن الحقول التي يتحكم بها المستخدم (تسمية المصدر، الوسوم، النص، الرابط) في الإعادات التلقائية للتوجيه ورسالة تأكيد صندوق الوارد تُهرَّب الآن بـ `html.escape`. سابقاً، كان وجود `<` أو `>` أو `&` يسبّب خطأ محلّل في Telegram، ونتيجة لذلك كان العنصر يفشل بصمت في الوصول إلى قناة الأرشيف.
- اختبار جديد في `tests/test_autoforward.py` يتحقق من تهريب هذه الأحرف.

## الإعادة التلقائية للتوجيه إلى قناة أرشيف (2026-06-11)

الإعادة التلقائية لتوجيه العناصر الموسومة إلى قناة أرشيف (إحدى متابعات محرّك القواعد).

### السلوك
- الأمر `/setarchive <@channel | chat id>` يعيّن قناة الأرشيف الخاصة بالمستخدم؛ و`/setarchive off` يعطّلها، و`/setarchive` دون وسيطة يعرض الحالة الحالية.
- في مسار صندوق الوارد للرسائل المُعاد توجيهها، بعد حفظ ناجح، يُفحَص النص المُعاد توجيهه مقابل قواعد المستخدم (`match_tags`)؛ وإذا طابق وسم واحد على الأقل وكانت قناة أرشيف معيَّنة، يُعاد توجيه العنصر إلى قناة الأرشيف مع مصدره ووسومه ونصه ورابطه. ويُسجَّل خطأ الإرسال بصمت ولا يكسر المسار الرئيسي.

### البيانات
- عمود `archive_chat_id` جديد على `bot_users` مع الترحيل المتماثل `_ensure_bot_user_columns` (ALTER TABLE إذا كان العمود مفقوداً). الطريقة `Repository.set_archive_chat`.

### خارج النطاق (متابعة لاحقة)
- القواعد القائمة على الذكاء الاصطناعي والإعادة التلقائية للتوجيه لعمليات استيراد القنوات (في الوقت الحالي، صندوق الوارد للرسائل المُعاد توجيهها فقط).

### الاختبارات
- `tests/test_autoforward.py`: قرار/تنسيق `_auto_forward` (الإرسال عند وجود أرشيف+وسم، التخطّي عند غياب أيّهما، ابتلاع أخطاء الإرسال)، دورة `/setarchive` (تعيين/عرض/مسح)، وترحيل العمود + النطاق لكل مستخدم.

## تجميع المواضيع (2026-06-11)

تجميع محتوى الأرشيف حسب المواضيع (إحدى متابعات NotebookLM).

### السلوك
- وحدة `clustering.py` جديدة (Python نقية، دون تبعيات): تجميع جشِع بمرور واحد قائم على تشابه جيب التمام مع نقاط مركزية متحركة، و`top_terms` لبناء تصنيف عنقود من أكثر المصطلحات الدالّة تكراراً (مع قائمة كلمات توقف متعددة اللغات). ولأن المقاطع لها تضمينات مخزَّنة، فإنه يعمل دون اتصال بالكامل.
- `Repository.chunks_with_embeddings` يعيد المقاطع التي لها تضمين (مقيّدة بنطاق المالك + المصدر/الوسم) ويفكّ ترميز الـ BLOB.
- أمر البوت `/topics [--source <url>] [--tag <tag>]` وأداة MCP `list_topics`.
- جرى تحديث `/help` وREADME.

### خارج النطاق (متابعة لاحقة)
- تسمية العناقيد بنموذج لغوي كبير وخط زمني تلقائي.

### الاختبارات
- `tests/test_clustering.py`: `top_terms`، فصل العناقيد، حد العناقيد، رفض العناصر دون تضمين، تصنيف/ترتيب `build_topics`، فك الترميز والنطاق في `chunks_with_embeddings`، وأداة MCP `list_topics`.

## CI — pytest + ruff (2026-06-11)

إضافة خط أنابيب تكامل مستمر (CI) كي لا يصل الكود المعطوب إلى `main`؛ سابقاً، كانت GitHub Actions تعالج عمليات النشر فقط.

### CI
- سير عمل جديد `.github/workflows/ci.yml` عند كل push و pull_request: تثبيت التبعيات، ثم `ruff check` و`pytest`.
- تُشغَّل المجموعة الكاملة (73 اختباراً) في CI؛ و`test_telegram_client` ينجح أيضاً دون تشغيل Telethon فعلياً (الاستيرادات كسولة).

### Lint
- إعداد `ruff` في `pyproject.toml` (مجموعة القواعد `E,F,I,UP,B`؛ `line-length=140`) وإضافة `ruff` إلى تبعيات التطوير.
- إصلاح جميع ملاحظات الفحص: إزالة الاستيرادات غير المستخدمة، فرز الاستيرادات، `datetime.UTC`، `zip(..., strict=True)` في مسارات التشفير/جيب التمام، `raise ... from` في كتل except، وتعليق توضيحي آمن لـ `TelegramClient` تحت `TYPE_CHECKING`.

### التشغيل محلياً
```bash
pip install -e ".[dev]"
ruff check src/ tests/
pytest -q
```

## المرحلة 8 — خادم MCP (2026-06-09)

المرحلة الأخيرة من خارطة الطريق: خادم MCP للقراءة فقط كي يمكن ربط أرشيف Telegram الخاص بالمستخدم بأدوات ذكاء اصطناعي أخرى.

### السلوك
- وحدة `mcp_server.py` جديدة: JSON-RPC 2.0 على stdio، باستخدام المكتبة القياسية فقط (دون تبعيات جديدة). `handle_request` دالة نقية dict→dict، و`serve_stdio` حلقة رفيعة مفصولة بأسطر جديدة فوقها.
- طرق البروتوكول: `initialize` (protocolVersion، serverInfo، capabilities.tools)، `notifications/initialized` (دون استجابة)، `tools/list`، `tools/call`.
- الأدوات (كلها للقراءة فقط): `list_sources`، `list_tags`، `search_telegram_archive` (مع مرشّح للمصدر/الوسم)، `get_message` (النص الكامل لعنصر حسب `media_item_id`)، `ask_telegram_notebook` (RAG)، `summarize_source`.
- مقيّدة بمالك واحد من `MCP_OWNER_ID` (الافتراضي `0` = أرشيف الويب). تمرّ جميع الاستعلامات عبر عزل `owner_id`.
- التشغيل: `python -m telegram_notebook.mcp_server`.

### Repository
- طريقة جديدة `get_media_item(owner_id, media_item_id)` لأداة `get_message`.

### الاختبارات
- `tests/test_mcp_server.py`: initialize/tools-list، سلوك الإشعار، خطأ الطريقة غير المعروفة، list_sources/search/get_message، أداة غير معروفة (isError)، العزل لكل مالك، ودورة `serve_stdio` كاملة ذهاباً وإياباً.

## المرحلة 7 — الملخصات / NotebookLM (2026-06-09)

تلخيص الأرشيف من خارطة الطريق (ملخص لكل مصدر ولكل وسم).

### السلوك
- `/summarize [--source <url>] [--tag <tag>]` — بدون مرشّح، يُلخَّص الأرشيف بأكمله؛ ومع `--source`، مصدر واحد؛ ومع `--tag`، وسم واحد (باستخدام محلّل `_split_filters` نفسه).
- يُجلَب المحتوى (صف واحد لكل عنصر، مع النص والمصدر) من `Repository.summary_items` (مقيّد بنطاق المالك + المصدر/الوسم، بحد افتراضي 200 عنصر).
- يُبنى الملخص بـ `SearchService.summarize`؛ ويُنتَج الموجّه في `_build_summary_prompt` (دالة نقية) مع ذكر المصادر وتقصير نص كل عنصر، ويُمرَّر إلى `gemini_generate_content`.

### خارج النطاق (متابعة لاحقة)
- تجميع المواضيع وخط زمني تلقائي.

### الاختبارات
- `tests/test_summarize.py`: بناء الموجّه (بما في ذلك المصادر والنطاق، تقصير النص)، الرسالة الفارغة، ونطاق الطريقة `summary_items` (الكل/الوسم/المصدر والعزل لكل مستخدم).

## المرحلة 6 — مهام الاستيراد الكامل (2026-06-09)

الاستيراد الكامل للقناة من خارطة الطريق: طابور انتظار، عامل في الخلفية، تتبع التقدّم، الاستئناف بعد الانقطاع، والإلغاء.

### نموذج البيانات
- جدول `jobs` (`owner_id`، `channel_url`، `status`، `total`، `processed`، `cursor`، `limit_count`، `error`، `cancel_requested`، الطوابع الزمنية). status هي إحدى `queued|running|done|failed|cancelled`.
- طرق Repository: `create_job`، `get_job`، `list_jobs`، `claim_next_queued_job` (اختيار ذرّي لأقدم مهمة ونقلها إلى running)، `update_job_progress`، `finish_job`، `request_job_cancel`، `is_cancel_requested`، و`requeue_running_jobs` (إعادة المهام running اليتيمة بسبب عامل متعطّل إلى queued).

### العامل (Worker)
- وحدة `jobs.py` جديدة مع `JobWorker` (خيط daemon واحد). وهي مفصولة عن Telegram وتعمل مع `runner` محقون بحيث تكون آلة الحالة قابلة للاختبار بالكامل بالوحدة.
- عند بدء التشغيل، تعيد إدراج المهام running اليتيمة (الاستئناف بعد التعطّل).

### خط المعالجة (Pipeline)
- اكتسب `ingest_channel` المعاملات `resume_from` (min_id للمتابعة)، `progress_cb(processed, total, last_msg_id)`، و`should_cancel()`. ولكل رسالة، يُفحَص الإلغاء ويُحدَّث التقدّم/المؤشر. ولأن التخزين متماثل، فإن الاستئناف آمن.
- اكتسب `iter_all_messages` معاملاً `min_id`، وأصبح `limit` الآن اختيارياً (`None` = جميع الرسائل).

### البوت
- `/import <channel_url> [limit]` (وضع استيراد كامل/قابل للاستئناف في طابور الانتظار)، `/jobs` (الحالة والتقدّم)، `/canceljob <id>`.
- يبقى `/ingest` المسار المباشر السريع. ويُبدأ العامل في `run_forever`، وعند نهاية كل مهمة، يرسل رسالة done/failed/cancelled إلى المستخدم.
- جرى تحديث `/help`.

### الاختبارات
- `tests/test_jobs.py`: دورة حياة المهمة، المطالبة الذرّية والترتيب، التقدّم/الإلغاء/إعادة الإدراج، وآلة حالة العامل مع runner وهمي (done/failed/cancelled وتقدّم المؤشر للاستئناف).

## المرحلة 5 — القواعد + الوسوم (2026-06-09)

محرّك القواعد ونظام الوسوم من خارطة الطريق. يعرّف المستخدم قاعدة بصيغة كلمة مفتاحية←وسم ويُوسَم المحتوى الوارد تلقائياً ويمكن تصفيته في البحث/السؤال.

### نموذج البيانات
- جدول `rules` (`owner_id`، `keyword`، `tag`، `created_at`) مع فهرس فريد على `(owner_id, keyword, tag)`.
- جدول `content_tags` (`owner_id`، `media_item_id`، `tag`) مع مفتاح أساسي مركّب (وسم متماثل).
- كلاهما يُنشَأ بـ `CREATE TABLE IF NOT EXISTS`؛ لا حاجة إلى ترحيل خاص لقواعد البيانات الموجودة.

### المطابقة والوسم التلقائي
- وحدة `rules.py` جديدة مع الدالة النقية `match_tags(text, rules)` (نص فرعي، غير حساس لحالة الأحرف).
- في مسارات الاستيعاب الثلاثة كلها (نص القناة، تفريغ الوسائط، صندوق الوارد للرسائل المُعاد توجيهها)، بعد تخزين النص يطبّق خط المعالجة قواعد المالك ويخزّن الوسوم (`_apply_rules`). وأُضيف `owner_id` إلى المساعدات الداخلية لخط المعالجة.

### أوامر البوت
- `/rule add <keyword> -> <tag>`، `/rule list`، `/rule remove <id>`، و`/rule apply` (مسح وإعادة حساب الوسوم من النصوص المخزَّنة).
- `/tags` — الوسوم وعدد العناصر المتمايز لكل وسم.
- مرشّح `--tag <tag>` لـ `/search` و`/ask`. واستُبدِل المحلّل `_split_source` بـ `_split_filters`، الذي يفهم كلاً من `--source` (رمز واحد) و`--tag` (إلى نهاية السطر، متعدد الكلمات).
- جرى تحديث `/help`.

### البحث
- اكتسب `SearchService.search` معاملاً `tag`. يُصفّى مسار الكلمات المفتاحية بربط على `content_tags`؛ ويُصفّى المسار الدلالي (Vertex) لاحقاً بقائمة مسموح بها من `media_ids_for_tag`.

### الاختبارات
- `tests/test_rules.py`: المطابقة النقية، تحليل `/rule add`، CRUD القواعد والتفرّد، تخزين/عدّ الوسوم، الوسم التلقائي عند الاستيعاب، البحث المصفّى بالوسم، والتعبئة الرجعية.
- `tests/test_normalize.py`: اختبار `_split_filters` (بدلاً من `_split_source`).

## المرحلة 4 — صندوق الوارد للرسائل المُعاد توجيهها (MVP) (2026-06-09)

تنفيذ المرحلة التالية من خارطة الطريق: "صندوق الوارد الذكي لـ Telegram". يمكن للمستخدم الآن إعادة توجيه أي رسالة إلى البوت، ويُخزَّن نصها/تسميتها التوضيحية في صندوق وارد شخصي قابل للبحث.

### السلوك
- يكتشف البوت الرسائل المُعاد توجيهها (كلٌّ من الصيغة الجديدة `forward_origin` والحقول القديمة مثل `forward_from`/`forward_from_chat`/`forward_sender_name`) ويوجّهها قبل منطق مسار التوثيق، كي لا تتعارض مع الردود النصية في مسار `/connect`.
- يُخزَّن `text` أو `caption` الخاص بالرسالة المُعاد توجيهها، إلى جانب وسم نوع الوسائط (مثل `[Forwarded document: report.pdf]`) والمصدر (اسم القناة/المستخدم الأصلي).
- عندما يكون المصدر قناة عامة، يُبنى الرابط `https://t.me/<username>/<id>` كمصدر.
- المحتوى المخزَّن قابل للاستعلام عبر نفس `/search` و`/ask` (مقطع + تضمين، مع عودة إلى الكلمات المفتاحية إذا لم يتوفر تضمين).

### نموذج البيانات
- يُنفَّذ صندوق الوارد كـ "قناة" اصطناعية لكل مستخدم بـ `channel_url = inbox://forwarded`، مع إعادة استخدام المخطط ومسار البحث الموجودين (وعزل `owner_id` من المرحلة 2).
- طريقة جديدة `IngestionPipeline.ingest_forwarded_message` (متماثلة بناءً على message_id للرسالة المُعاد توجيهها).

### تجربة مستخدم البوت
- جرى تحديث `/start` و`/help` لشرح قدرة إعادة التوجيه.
- رسالة إرشادية للعناصر التي تحتوي وسائط فقط دون نص (وهي غير مفهرسة بعد في هذه النسخة).
- إعادة هيكلة: جُمِّع إعداد Vertex المتعلق بالفهرسة في مساعد مشترك (`_vertex_ingest_config`) كي يستخدمه `/ingest` وصندوق الوارد كلاهما.

### خارج النطاق (متابعة لاحقة)
- تنزيل وتفريغ الوسائط المُعاد توجيهها عبر Bot API، OCR للصور، واستخراج النص من PDF/DOCX/Excel.

### الاختبارات
- `tests/test_forwarded.py`: كشف الإعادة، استخراج المصدر/الرابط/وسم الوسائط، والاستيعاب من طرف إلى طرف (التخزين والقابلية للبحث، التماثل، والطبيعة الخاصة بكل مستخدم لصندوق الوارد).

## المرحلة 3 — توثيق Web API وتشفير الأسرار (2026-06-09)

العنصران الأمنيان المتبقيان من التحليل: توثيق Web API وتشفير الأسرار في قاعدة البيانات.

### توثيق Web API
- متغير جديد `WEB_API_TOKEN`. عند تعيينه، تتطلب جميع نقاط النهاية `/api/*` (باستثناء `/api/health`) الرمز؛ ويُرسَل الرمز عبر `Authorization: Bearer <token>` أو ترويسة `X-API-Token` (مقارنة بزمن ثابت).
- عند عدم تعيين رمز، يقبل الـ API طلبات loopback (localhost) فقط، ويُرفَض الوصول الشبكي غير الموثَّق بـ 401 (آمن افتراضياً؛ سابقاً كان مفتوحاً تماماً).
- يبقى `/api/health` عاماً لفحص صحة Docker.
- واجهة لوحة التحكم: تمرّ جميع الاستدعاءات عبر `fetchJson`؛ ترسل هذه الدالة الرمز من `localStorage` وعند استجابة 401، تطلب من المستخدم رمزاً مرة واحدة وتخزّنه.

### تشفير الأسرار أثناء التخزين
- وحدة `crypto.py` جديدة: تشفير موثَّق باستخدام المكتبة القياسية فقط (فصل المفاتيح بـ HKDF-SHA256، تدفّق مفاتيح بـ HMAC-SHA256 في وضع CTR، وEncrypt-then-MAC بـ HMAC-SHA256؛ nonce عشوائي بـ 128 بت لكل قيمة). دون تبعيات جديدة.
- تُشفَّر الأعمدة الحساسة قبل التخزين في SQLite: في `bot_users` ← `api_hash`، `session_string`، `gemini_api_key`؛ وفي `auth_flows` ← `api_hash`، `session_string`، `phone_code_hash`. والقراءة (`get_bot_user`/`get_auth_flow`) تفكّ التشفير بشفافية.
- يُقرَأ المفتاح من `SECRETS_KEY`. وإذا لم يُعيَّن، يكون التشفير بلا عملية (مع تحذير) وتظل قواعد البيانات القديمة بالنص الصريح تعمل؛ وتُميَّز القيم المشفّرة عن النص الصريح القديم ببادئة `enc::` لجعل الترحيل سلساً.

### الاختبارات
- `tests/test_crypto.py`: الدورة ذهاباً وإياباً، اللاحتمية، رفض العبث/المفتاح الخاطئ، المرور لـ None/الفارغ/النص الصريح القديم، وسلوك بلا عملية دون مفتاح.
- `tests/test_web_auth.py`: قبول bearer/`X-API-Token`، رفض رمز خاطئ/مفقود، وقيد loopback عند عدم تعيين رمز.
- `tests/test_db.py`: اختبارات جديدة للتخزين المشفّر للأسرار وفك التشفير الشفاف عند القراءة.

### .env.example
- إضافة `WEB_API_TOKEN` و`SECRETS_KEY` إلى جانب الأمر لتوليد قيمة.

## المرحلة 2 — عزل البيانات لكل مستخدم (2026-06-09)

تركيز هذه المرحلة هو إصلاح تسرّب البيانات بين المستخدمين: سابقاً، كان `/search` و`/ask` (والـ Web API) يعملان على **جميع** القنوات في قاعدة البيانات، وكان بإمكان المستخدمين رؤية بيانات بعضهم البعض.

### نموذج البيانات
- أُضيف عمود `owner_id` إلى جدول `channels` وتُفرَض الملكية على هذا المستوى؛ ولأن كل `message`/`media_item`/`chunk` مرتبط بقناة عبر FK، فإن التصفية على `channels.owner_id` في الربطات تعزل البيانات تماماً.
- استُبدِل القيد العام `UNIQUE(channel_url)` بفهرس مركّب `UNIQUE(owner_id, channel_url)` كي يتمكن مستخدمان من استيعاب القناة نفسها بشكل مستقل دون مشاركة صف.
- ترحيل تلقائي (`Repository._ensure_channel_owner`) لقواعد البيانات القديمة: يُعاد بناء جدول `channels`، ويُضاف عمود `owner_id`، وتُحفَظ الصفوف القديمة بـ `owner_id = NULL`؛ أي أنها بدلاً من التسرّب بين المستخدمين، تصبح غير مرئية للاستعلامات لكل مستخدم (ويجب إعادة استيعابها إذا لزم الأمر).

### فرض النطاق
- طرق Repository التي تعيد أو تحذف بيانات تأخذ الآن `owner_id`: `upsert_channel`، `keyword_candidates`، `embedding_candidates`، `list_channels`، `delete_channel_data`، `get_chunk_by_media_and_index`.
- `SearchService.search` و`IngestionPipeline.ingest_channel` يأخذان معامل `owner_id`.
- يمرّر بوت Telegram `bot_user_id` الخاص بالمستخدم كـ `owner_id`؛ ولذلك `/search` و`/ask` و`/ingest` و`/sources` و`/delete` و`/status` تعمل فقط على بيانات ذلك المستخدم.
- لوحة التحكم على الويب (التي ليس لها تسجيل دخول لكل مستخدم) تستخدم `WEB_OWNER_ID = 0` ثابتاً كي يبقى أرشيفها منفصلاً عن أرشيفات مستخدمي البوت.

### التقوية
- يُربَط `LIMIT` في `keyword_candidates` الآن كمعامل بدلاً من الاستيفاء بالسلاسل النصية.

### الاختبارات
- جرى تحديث اختبارات `Repository` لتمرير `owner_id`.
- اختبار جديد `test_data_is_isolated_per_owner`: مستخدمان بالرابط نفسه لا يريان بيانات بعضهما، وحذف بيانات أحدهما لا يؤثر على بيانات الآخر.
- اختبار جديد `test_migrates_legacy_channels_table_without_owner_id`: ترحيل قاعدة بيانات قديمة دون `owner_id`.

## المرحلة 1 — تثبيت النواة (2026-06-08)

وفقاً لخارطة الطريق في README، تركّز هذه المرحلة على تثبيت النواة: الأمن، إصلاح الأخطاء، أوامر البوت، التسجيل، والاختبارات.

### الأمن
- أُزيل رمز البوت الحقيقي من `.env.example` وأُفرِغ.
  - ⚠️ سبق إيداع هذا الرمز في سجلّ git (الإيداع `5501fda`) وهو فعلياً علني. إفراغ الملف لا يكفي؛ يجب أن تنفّذ فوراً `/revoke` للرمز في **@BotFather** وتنشئ واحداً جديداً.
- أُفرِغت المعرّفات الخاصة بالبيئة (`VERTEX_INDEX_ID`، `VERTEX_DEPLOYED_INDEX_ID`) في ملف المثال.

### إصلاح الأخطاء
- `/search` و`/ask`: يُقرَأ المستخدم بـ `bot_user_id` الحقيقي، وليس `chat_id` (في المجموعات يختلف الاثنان).
- Web API: `/api/search` و`/api/ask` يمرّران الآن `vertex_config` (و`project_id`/`region` لـ ask)؛ سابقاً كانا يعودان دائماً إلى بحث الكلمات المفتاحية.
- استجابة `/ask` في البوت تستخدم `<b>` (HTML) كي تُعرَض بشكل صحيح مع `parse_mode=HTML` (سابقاً كانت `**` خاماً).
- جرى مواءمة افتراضي `DB_PATH` في `.env.example` مع `config.py`: `data/store.db`.

### أوامر بوت جديدة
- `/status` — حالة الاتصال، مفتاح الذكاء الاصطناعي، إعداد Vertex، وعدد المصادر المفهرسة.
- `/disconnect` — حذف جلسة المستخدم وبيانات اعتماده ("احذف بياناتي").
- `/help` — قائمة الأوامر.
- لم تعد الأوامر تعاني من لاحقة `@botname` وحالة الأحرف الكبيرة/الصغيرة، ولم تعد تدخل مسار الاتصال خطأً.
- حارس للإدخال الفارغ في `/search` و`/ask` و`/ingest` و`/join` و`/delete`.

### التسجيل ومعالجة الأخطاء
- وحدة `logging_config.py` جديدة مع `setup_logging()` (المستوى من `LOG_LEVEL`، الافتراضي INFO).
- استُبدِلت جميع `print()` للتصحيح بـ `logging`؛ ولم تعد القيم الحساسة (رقم الهاتف، رمز الدخول، `phone_code_hash`) تُسجَّل.
- لم يعد تحديث واحد معطوب يوقف حلقة استطلاع البوت بأكملها (يُسجَّل ويستمر التنفيذ).

### الاختبارات
- مجموعة `tests/` مع pytest؛ 26 اختباراً دون حاجة إلى شبكة: `chunking`، تشابه جيب التمام، `normalize_phone`/`normalize_code`، الرابط القانوني، تكوين النص، تطهير اسم القناة، `Repository` على SQLite مؤقت، و`upsert_env_values`.
- التشغيل: `pip install -e ".[dev]"` ثم `pytest`.

### المتابعات (لمراحل لاحقة)
- لا يزال `normalize_phone` ساذجاً للأرقام ذات رمز الدولة (مثل `09123456789` → `+09123456789`).
- يبقى `import re` في `bot.py` غير مستخدم بعد إزالة التعبيرات النمطية ويمكن تنظيفه.
- لا يزال `main.py` يبني حالة عامة عند وقت الاستيراد؛ والأفضل جعلها كسولة.
- لم يُنجَز بعد عزل البيانات لكل مستخدم (المرحلة 2): `/search` و`/ask` يعملان على جميع القنوات، وليس على بيانات ذلك المستخدم فقط.
