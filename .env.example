APP_NAME=Telegram Notebook
DATA_DIR=data
DB_PATH=data/store.db
MEDIA_DIR=data/media

TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_SESSION_STRING=
TELEGRAM_SESSION_NAME=telegram-notebook
# Get this from @BotFather. NEVER commit a real token to the repo.
TELEGRAM_BOT_TOKEN=

# ---------------------------------------------------------------------------
# AI providers — LOCAL-FIRST by default (no cloud keys required).
#
# Chat/RAG + embeddings run on a local Ollama server, transcription on a local
# Whisper model. OpenAI/Gemini stay available: set a key below and switch the
# matching *_PROVIDER to "openai" or "gemini".
#
# One-time local setup:
#   1) Install Ollama from https://ollama.com and run:  ollama serve
#   2) ollama pull llama3.1            # chat / RAG / summaries / auto-tag
#      ollama pull nomic-embed-text    # embeddings
#   3) For local transcription:        pip install ".[local]"
# ---------------------------------------------------------------------------

# Chat / RAG / summaries / auto-tagging. provider: "ollama" (default), "openai", "gemini".
LLM_PROVIDER=ollama
LLM_MODEL=llama3.1
OLLAMA_BASE_URL=http://localhost:11434

# Embeddings. provider: "ollama" (default), "openai", "gemini".
EMBEDDING_PROVIDER=ollama
EMBEDDING_MODEL=nomic-embed-text

# Transcription (audio/video -> text). provider: "local" (Whisper, default),
# "openai", "gemini". For "local", TRANSCRIPTION_MODEL is a Whisper size
# (tiny | base | small | medium | large-v3).
TRANSCRIPTION_PROVIDER=local
TRANSCRIPTION_MODEL=base

# Optional cloud keys (only needed if you switch a *_PROVIDER above to them).
OPENAI_API_KEY=
GEMINI_API_KEY=

CHUNK_SIZE=900
CHUNK_OVERLAP=120
DEFAULT_RESULT_LIMIT=8

# Audio Overview / podcast (/podcast). Needs the optional extra: pip install ".[podcast]"
# TTS engine: "edge" (free, no key — default), "openai", "gemini", or "elevenlabs".
PODCAST_TTS_MODEL=edge
# Optional override for the LLM that writes the conversation (e.g. gpt-4o-mini).
# Leave blank to use Podcastfy's default (Gemini when GEMINI_API_KEY is set).
PODCAST_LLM_MODEL=

# Telegram-Toolset: run a deleted-message watcher for the web's server account.
# When 1/true, the web app keeps a live client on TELEGRAM_SESSION_STRING that
# caches incoming messages and recovers deleted ones (visible at /api/deleted).
# Only messages received after it starts can be recovered. Default off.
WEB_WATCH_DELETED=

# Security
# Token required to call the web API (/api/*) from non-local clients. When set, send it
# as "Authorization: Bearer <token>" or "X-API-Token: <token>". When empty, the API only
# accepts loopback (localhost) requests. Generate one with e.g.:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
WEB_API_TOKEN=
# Telegram Mini App: public HTTPS URL where /miniapp is served (e.g. behind a
# reverse proxy or tunnel). When set, the bot shows an "Open App" menu button and
# the /app command; the Mini App authenticates users via signed Telegram initData.
MINIAPP_URL=
# Master key used to encrypt secrets (Telegram session strings, API hashes, API keys)
# at rest in the database. When empty, secrets are stored UNENCRYPTED. Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
# Keep this stable: changing it makes previously stored secrets unreadable (re-login needed).
SECRETS_KEY=

# Vertex AI Search (leave blank to use local keyword/cosine search only)
VERTEX_PROJECT_ID=
VERTEX_REGION=us-central1
VERTEX_INDEX_ID=
VERTEX_ENDPOINT_ID=
VERTEX_DEPLOYED_INDEX_ID=

# Telegram Proxy (If needed for connection issues)
TELEGRAM_PROXY_HOST=
TELEGRAM_PROXY_PORT=
TELEGRAM_PROXY_TYPE=socks5

# --- NabuGate: the only AI credential this project should need -----------------
# Mint a project token at https://gate.nabuxai.com/admin/ (Tokens -> + new).
# It is shown once. Scope it to: nabu-fast, nabu-smart, notebook-embed.
#
# With this set, LLM_PROVIDER and EMBEDDING_PROVIDER both default to "nabugate"
# and OPENAI_API_KEY / GEMINI_API_KEY below stop being read. Leave it empty and
# the project keeps its previous Ollama-first behaviour unchanged.
NABUGATE_BASE_URL=https://gate.nabuxai.com/v1
NABUGATE_API_KEY=
